# CMS新闻站引擎项目总结

## 项目概述

本项目是一个基于.NET 8的新闻站引擎，采用模块化单体架构，专注于内容管理和页面渲染的一体化解决方案。系统设计遵循"固定模板、固定模型、简化运营动作、强化发布稳定性、优先保障SEO与性能"的原则，目标是构建一个稳定可运营的新闻站引擎。

## 项目定位

- **面向资讯站/新闻站**
- **内容管理 + 页面渲染一体化**
- **模板固定，改动频率低**
- **编辑人员主要关注：标题、摘要、正文、头图/视频、栏目、标签、发布时间**
- **系统重点追求：稳定、易运营、易维护、SEO友好、前台性能好**

一句话概括：做一套"稳定可运营的新闻站引擎"，而不是一个大而全的平台。

## 技术架构

### 技术栈
- **框架**: .NET 8 / ASP.NET Core 8
- **前台**: ASP.NET Core MVC + Razor (服务端渲染)
- **后台接口**: ASP.NET Core Web API
- **管理后台前端**: Vue + Vue Router + Pinia + Axios + Element Plus/Ant Design Vue
- **数据访问**: Entity Framework Core + Dapper
  - **EF Core**: 常规增删改查、事务型业务、实体映射
  - **Dapper**: 后台复杂列表查询、前台高性能只读查询、统计/聚合查询
- **数据库**: SQL Server 或 MySQL
  - **SQL Server**: 偏.NET传统企业栈，更顺手
  - **MySQL**: 更看重成本和Linux部署便利
- **缓存**: MemoryCache (单机部署，不需要Redis)
- **富文本编辑器**: CKEditor 4.16.2
- **前端框架**: Bootstrap 5.3

### 项目结构
```
Cms/
├── Cms.Domain/           # 领域层 - 实体、值对象、枚举、核心业务规则
├── Cms.Infrastructure/    # 基础设施层 - EF Core DbContext、Repository实现、Dapper查询实现、文件存储、缓存实现
├── Cms.Application/      # 应用服务层 - 用例编排、Command/Query、DTO、接口定义、权限校验编排、发布流程编排、富文本清洗、缓存清理编排
├── Cms.Web/            # 前台网站 - 新闻展示、网站路由、前台页面渲染、SEO页面输出
├── Cms.WebApi/         # 后台接口 - 为Vue管理后台提供API、登录认证、用户/角色/权限管理、栏目/文章/标签/专题/推荐位/资源管理
├── Cms.Admin/          # 后台管理 - 内容管理（Vue前端）
└── Cms.Shared/         # 共享层 - 公共常量、基础枚举、通用返回结构、公共异常、公共工具接口
```

## 核心功能模块

### 1. 数据库模型设计
- **CmsWebsite**: 站点表，支持多域名站群
- **CmsChannel**: 栏目表，支持多级栏目结构
- **CmsArticle**: 文章主表，存储文章基础信息
- **CmsArticleContent**: 文章内容表，存储HTML和纯文本内容
- **CmsTag**: 标签表
- **CmsArticleTag**: 文章标签关联表
- **CmsTopic**: 专题表
- **CmsTopicArticle**: 专题文章关联表
- **CmsMediaAsset**: 媒体资源表
- **CmsRecommendSlot**: 推荐位表
- **CmsRecommendItem**: 推荐项表
- **CmsUser**: 用户表
- **CmsRole**: 角色表
- **CmsPermission**: 权限表
- **CmsUserRole**: 用户角色关联表
- **CmsRolePermission**: 角色权限关联表
- **CmsOperationLog**: 操作日志表

### 2. Website管理模块
- 站点名称、域名配置
- 站点Logo、默认SEO配置
- 页脚信息管理
- 站点启用/禁用控制
- 支持多域名站群

### 3. 栏目管理模块
- 栏目树维护
- 栏目排序
- 栏目启用/停用
- 栏目SEO
- 栏目slug
- 栏目模板类型
- 是否显示在导航
- 技术要点：栏目是新闻站骨架，建议控制在2~3级，栏目查询要做缓存，URL规则要提前固定

### 4. 文章管理模块
- 文章新增/编辑
- 草稿保存
- 发布
- 下线
- 删除
- 置顶/推荐/头条
- 封面图/视频
- 作者/来源/摘要/标签
- SEO字段
- 建议拆表：Articles、ArticleContents
- 技术要点：主表存轻字段，正文单独拆表，支持草稿与发布态分离，列表查询优先用Dapper，保存时做富文本清洗

### 5. 标签管理模块
- 标签维护
- 标签与文章关联
- 标签页聚合展示
- 技术要点：标签独立表，中间关系表单独建，标签页可作为SEO聚合页

## 标签管理模块详细需求Prompt

### 1. 功能需求

#### 1.1 标签基础管理
- **标签创建**：支持创建新标签，包括标签名称和slug
- **标签编辑**：支持修改现有标签的名称和slug
- **标签删除**：支持软删除标签（标记IsDeleted为true）
- **标签列表**：支持分页查询标签列表，可按关键词搜索
- **标签详情**：支持查看标签详情，包括标签名称、slug、网站ID和关联文章数量

#### 1.2 标签与文章关联
- **文章标签管理**：支持为文章添加标签
- **标签文章列表**：支持查看标签关联的文章列表
- **标签使用统计**：支持统计标签的使用次数

#### 1.3 标签前台展示
- **标签云**：在前台展示热门标签
- **标签聚合页**：根据标签slug展示相关文章列表
- **SEO优化**：标签页支持SEO meta信息配置

### 2. 技术实现

#### 2.1 数据模型
- **CmsTag**：标签实体，包含Id、Name、Slug、WebsiteId、IsDeleted、CreatedAt、UpdatedAt等字段
- **CmsArticleTag**：文章标签关联实体，包含Id、ArticleId、TagId、IsDeleted、CreatedAt、UpdatedAt等字段

#### 2.2 服务层
- **ITagService**：标签服务接口，定义标签相关的业务逻辑方法
- **TagService**：标签服务实现，包含标签的CRUD操作、文章标签关联管理、标签统计等功能

#### 2.3 API接口
- **GET /api/tags/{id}**：根据ID获取标签详情
- **GET /api/tags**：获取标签列表，支持分页和关键词搜索
- **POST /api/tags**：创建标签
- **PUT /api/tags/{id}**：更新标签
- **DELETE /api/tags/{id}**：删除标签
- **GET /api/tags/all/{websiteId}**：获取所有标签列表
- **GET /api/tags/article/{articleId}**：获取文章标签
- **POST /api/tags/article/{articleId}**：为文章添加标签
- **DELETE /api/tags/article/{articleId}**：从文章中移除所有标签
- **GET /api/tags/count/{websiteId}**：获取带使用次数的标签

#### 2.4 前台实现
- **标签云组件**：在前台展示热门标签，按使用次数排序
- **标签聚合页**：根据标签slug展示相关文章列表，支持分页
- **文章详情页**：展示文章的标签列表，点击标签可跳转到标签聚合页

### 3. 数据隔离
- 所有标签查询和操作都必须按WebsiteId进行隔离
- 标签缓存键必须包含WebsiteId，例如：website:1:tags, website:1:tag:dotnet

### 4. 缓存策略
- 标签列表和标签云数据需要缓存
- 文章发布或更新时，需要清理相关标签的缓存
- 标签创建、编辑、删除时，需要清理标签相关的缓存

### 5. 前端实现
- **管理后台**：使用Vue实现标签管理界面，包括标签列表、创建、编辑、删除功能
- **前台展示**：使用Razor视图实现标签云和标签聚合页

### 6. 测试策略
- **单元测试**：测试标签服务的核心方法
- **集成测试**：测试标签API接口的功能
- **功能测试**：测试标签管理的完整流程，包括标签创建、编辑、删除、与文章关联等
- **性能测试**：测试标签查询和缓存的性能

### 7. 技术要点
- 标签slug生成：自动将标签名称转换为URL友好的slug格式
- 标签去重：确保同一网站内标签名称唯一
- 软删除：标签删除采用软删除方式，保留历史数据
- 批量操作：支持批量为文章添加标签
- 缓存优化：合理使用缓存提高标签查询性能
- SEO友好：标签页URL采用slug格式，支持SEO优化

### 6. 专题管理模块
- 专题创建
- 专题内容聚合
- 专题SEO
- 专题前台展示
- 说明：专题不是标签，建议独立建模

## 专题管理模块详细需求Prompt

### 1. 功能需求

#### 1.1 专题基础管理
- **专题创建**：支持创建新专题，包括专题名称、slug、封面图片、描述、SEO信息等
- **专题编辑**：支持修改现有专题的各项信息
- **专题删除**：支持软删除专题（标记IsDeleted为true）
- **专题列表**：支持分页查询专题列表，可按关键词搜索
- **专题详情**：支持查看专题详情，包括专题基本信息和关联文章数量

#### 1.2 专题与文章关联
- **文章聚合**：支持为专题添加文章，实现内容聚合
- **文章排序**：支持设置专题内文章的排序顺序
- **专题文章列表**：支持查看专题关联的文章列表
- **文章管理**：支持从专题中移除文章

#### 1.3 专题前台展示
- **专题详情页**：根据专题slug展示专题详情和聚合文章
- **SEO优化**：专题页支持SEO meta信息配置
- **响应式设计**：专题页适配不同设备

### 2. 技术实现

#### 2.1 数据模型
- **CmsTopic**：专题实体，包含Id、Name、Slug、CoverImage、Description、WebsiteId、IsEnabled、SeoTitle、SeoDescription、SeoKeywords、IsDeleted、CreatedAt、UpdatedAt等字段
- **CmsTopicArticle**：专题文章关联实体，包含Id、TopicId、ArticleId、SortOrder、StartTime、EndTime、IsDeleted、CreatedAt、UpdatedAt等字段

#### 2.2 服务层
- **ITopicService**：专题服务接口，定义专题相关的业务逻辑方法
- **TopicService**：专题服务实现，包含专题的CRUD操作、文章关联管理、专题文章查询等功能

#### 2.3 API接口
- **GET /api/topics/{id}**：根据ID获取专题详情
- **GET /api/topics/slug/{slug}**：根据slug获取专题详情
- **GET /api/topics**：获取专题列表，支持分页和关键词搜索
- **POST /api/topics**：创建专题
- **PUT /api/topics/{id}**：更新专题
- **DELETE /api/topics/{id}**：删除专题
- **GET /api/topics/{id}/articles**：获取专题文章列表
- **POST /api/topics/{id}/articles**：为专题添加文章
- **DELETE /api/topics/{id}/articles**：从专题中移除所有文章

#### 2.4 前台实现
- **专题详情页**：展示专题信息和聚合文章列表
- **专题列表组件**：在前台展示专题列表
- **面包屑导航**：专题页支持面包屑导航

### 3. 数据隔离
- 所有专题查询和操作都必须按WebsiteId进行隔离
- 专题缓存键必须包含WebsiteId，例如：website:1:topics, website:1:topic:ai-2026

### 4. 缓存策略
- 专题列表和专题详情数据需要缓存
- 文章发布或更新时，需要清理相关专题的缓存
- 专题创建、编辑、删除时，需要清理专题相关的缓存

### 5. 前端实现
- **管理后台**：使用Vue实现专题管理界面，包括专题列表、创建、编辑、删除功能，以及专题文章关联管理
- **前台展示**：使用Razor视图实现专题详情页和专题列表

### 6. 测试策略
- **单元测试**：测试专题服务的核心方法
- **集成测试**：测试专题API接口的功能
- **功能测试**：测试专题管理的完整流程，包括专题创建、编辑、删除、文章关联等
- **性能测试**：测试专题查询和缓存的性能

### 7. 技术要点
- 专题slug生成：自动将专题名称转换为URL友好的slug格式
- 专题去重：确保同一网站内专题名称唯一
- 软删除：专题删除采用软删除方式，保留历史数据
- 文章排序：支持专题内文章的自定义排序
- 缓存优化：合理使用缓存提高专题查询性能
- SEO友好：专题页URL采用slug格式，支持SEO优化
- 响应式设计：专题页适配不同设备

### 7. 推荐位/广告位管理模块
- 首页焦点图
- 首页头条
- 栏目推荐
- 热门推荐
- 文末相关阅读
- 广告位管理
- 技术要点：推荐位独立建模，支持排序，支持生效时间，首页和栏目页内容不要写死在代码里

## 推荐位/广告位管理模块详细需求Prompt

### 1. 功能需求

#### 1.1 推荐位基础管理
- **推荐位创建**：支持创建新推荐位，包括推荐位名称、代码、类型、排序等
- **推荐位编辑**：支持修改现有推荐位的各项信息
- **推荐位删除**：支持软删除推荐位（标记IsDeleted为true）
- **推荐位列表**：支持分页查询推荐位列表，可按关键词搜索
- **推荐位详情**：支持查看推荐位详情，包括推荐位基本信息和关联项目数量

#### 1.2 推荐位项目管理
- **项目添加**：支持为推荐位添加推荐项目，关联文章
- **项目编辑**：支持修改推荐项目的排序、生效时间等
- **项目删除**：支持删除推荐项目
- **项目列表**：支持查看推荐位的项目列表
- **生效时间管理**：支持设置推荐项目的开始和结束时间，控制推荐内容的展示时段

#### 1.3 推荐内容前台展示
- **首页焦点图**：在首页展示焦点图轮播
- **首页头条**：在首页展示头条新闻
- **栏目推荐**：在栏目页展示推荐内容
- **热门推荐**：在各页面展示热门文章
- **文末相关阅读**：在文章详情页底部展示相关文章
- **广告位**：在指定位置展示广告内容

### 2. 技术实现

#### 2.1 数据模型
- **CmsRecommendSlot**：推荐位实体，包含Id、Name、Code、Type、SortOrder、IsEnabled、WebsiteId、IsDeleted、CreatedAt、UpdatedAt等字段
- **CmsRecommendItem**：推荐位项目实体，包含Id、SlotId、ArticleId、SortOrder、StartTime、EndTime、IsDeleted、CreatedAt、UpdatedAt等字段

#### 2.2 服务层
- **IRecommendService**：推荐服务接口，定义推荐位相关的业务逻辑方法
- **RecommendService**：推荐服务实现，包含推荐位的CRUD操作、项目管理、推荐文章查询等功能

#### 2.3 API接口
- **GET /api/recommend/slots**：获取推荐位列表，支持分页和关键词搜索
- **GET /api/recommend/slots/{id}**：根据ID获取推荐位详情
- **POST /api/recommend/slots**：创建推荐位
- **PUT /api/recommend/slots/{id}**：更新推荐位
- **DELETE /api/recommend/slots/{id}**：删除推荐位
- **POST /api/recommend/items**：添加推荐位项目
- **PUT /api/recommend/items/{id}**：更新推荐位项目
- **DELETE /api/recommend/items/{id}**：删除推荐位项目
- **GET /api/recommend/articles/{code}**：获取推荐文章，支持数量限制

#### 2.4 前台实现
- **推荐位组件**：在前台页面中展示推荐内容
- **焦点图轮播**：实现首页焦点图的轮播效果
- **推荐列表**：展示推荐文章列表

### 3. 数据隔离
- 所有推荐位查询和操作都必须按WebsiteId进行隔离
- 推荐位缓存键必须包含WebsiteId，例如：website:1:recommend:slot:home-banner, website:1:recommend:articles:home-banner:10

### 4. 缓存策略
- 推荐位列表和推荐文章数据需要缓存
- 文章发布或更新时，需要清理相关推荐位的缓存
- 推荐位创建、编辑、删除时，需要清理推荐位相关的缓存
- 推荐项目添加、编辑、删除时，需要清理相关推荐位的缓存

### 5. 前端实现
- **管理后台**：使用Vue实现推荐位管理界面，包括推荐位列表、创建、编辑、删除功能，以及推荐项目管理
- **前台展示**：使用Razor视图实现推荐位内容的展示，如焦点图轮播、推荐文章列表等

### 6. 测试策略
- **单元测试**：测试推荐服务的核心方法
- **集成测试**：测试推荐API接口的功能
- **功能测试**：测试推荐位管理的完整流程，包括推荐位创建、编辑、删除、项目管理等
- **性能测试**：测试推荐内容查询和缓存的性能

### 7. 技术要点
- 推荐位代码生成：确保推荐位代码唯一，用于前台调用
- 推荐项目排序：支持自定义排序，控制前台展示顺序
- 生效时间控制：根据开始和结束时间自动控制推荐内容的展示
- 缓存优化：合理使用缓存提高推荐内容查询性能
- 响应式设计：推荐内容展示适配不同设备
- 灵活性：支持不同类型的推荐位，满足不同场景的需求

### 8. 媒体资源管理模块
- 图片上传
- 视频上传或视频地址录入
- 文件类型校验
- 文件大小限制
- 简单分类
- 引用关系查询
- 技术要点：统一上传接口，独立资源表，资源是否被文章引用要可追踪

## 媒体资源管理模块详细需求Prompt

### 1. 功能需求

#### 1.1 媒体资源上传
- **图片上传**：支持上传图片文件，包括JPG、PNG、GIF等常见图片格式
- **视频上传**：支持上传视频文件，包括MP4、WebM等常见视频格式
- **视频地址录入**：支持直接录入视频URL地址，如YouTube、Vimeo等
- **文件类型校验**：上传时自动校验文件类型，确保只允许合法的媒体文件
- **文件大小限制**：支持设置文件大小上限，防止上传过大的文件

#### 1.2 媒体资源管理
- **资源列表**：支持分页查询媒体资源列表，可按类型、名称、上传时间等筛选
- **资源详情**：支持查看媒体资源的详细信息，包括文件名、大小、类型、上传时间等
- **资源编辑**：支持修改媒体资源的名称、描述、分类等信息
- **资源删除**：支持删除不需要的媒体资源
- **资源分类**：支持对媒体资源进行简单分类管理

#### 1.3 引用关系管理
- **引用查询**：支持查询媒体资源被哪些文章引用
- **引用统计**：支持统计媒体资源的引用次数
- **引用详情**：支持查看具体的引用位置和文章信息

#### 1.4 前台展示
- **图片展示**：在文章和专题中展示图片
- **视频播放**：在文章和专题中播放视频
- **资源预览**：在后台管理中支持资源预览

### 2. 技术实现

#### 2.1 数据模型
- **CmsMediaAsset**：媒体资源实体，包含Id、Name、FilePath、FileUrl、FileType、FileSize、Description、Category、WebsiteId、IsDeleted、CreatedAt、UpdatedAt等字段

#### 2.2 服务层
- **IMediaAssetService**：媒体资源服务接口，定义媒体资源相关的业务逻辑方法
- **MediaAssetService**：媒体资源服务实现，包含资源的上传、管理、查询等功能

#### 2.3 API接口
- **POST /api/media/upload**：上传媒体资源
- **GET /api/media**：获取媒体资源列表，支持分页和筛选
- **GET /api/media/{id}**：根据ID获取媒体资源详情
- **PUT /api/media/{id}**：更新媒体资源信息
- **DELETE /api/media/{id}**：删除媒体资源
- **GET /api/media/{id}/references**：查询媒体资源的引用关系
- **GET /api/media/categories**：获取媒体资源分类列表

#### 2.4 存储实现
- **本地存储**：将媒体文件存储在本地服务器的指定目录
- **文件路径管理**：生成唯一的文件路径，避免文件名冲突
- **URL生成**：生成可访问的媒体资源URL

### 3. 数据隔离
- 所有媒体资源查询和操作都必须按WebsiteId进行隔离
- 媒体资源缓存键必须包含WebsiteId，例如：website:1:media:list:1:10, website:1:media:123

### 4. 缓存策略
- 媒体资源列表数据需要缓存
- 媒体资源详情数据需要缓存
- 媒体资源上传、编辑、删除时，需要清理相关缓存

### 5. 前端实现
- **管理后台**：使用Vue实现媒体资源管理界面，包括资源上传、列表展示、编辑、删除功能
- **前台展示**：使用Razor视图实现媒体资源的展示，如文章中的图片和视频

### 6. 测试策略
- **单元测试**：测试媒体资源服务的核心方法
- **集成测试**：测试媒体资源API接口的功能
- **功能测试**：测试媒体资源管理的完整流程，包括上传、管理、引用查询等
- **性能测试**：测试媒体资源上传和查询的性能

### 7. 技术要点
- **文件上传**：使用ASP.NET Core的IFormFile处理文件上传
- **文件类型校验**：通过文件扩展名和MIME类型双重校验
- **文件大小限制**：在配置文件中设置文件大小上限
- **文件存储**：使用本地文件系统存储媒体文件
- **引用追踪**：通过数据库关系追踪媒体资源的引用情况
- **缓存优化**：合理使用缓存提高媒体资源查询性能
- **安全性**：防止文件上传漏洞，如恶意文件上传
- **响应式设计**：媒体资源管理界面适配不同设备

### 9. SEO管理模块
- 文章SEO
- 栏目SEO
- 专题SEO
- 默认SEO
- slug
- sitemap
- robots
- canonical
- 重定向规则（可后加）
- 技术要点：SEO是传统新闻站核心能力之一，URL要尽早固定，模板里不要写死SEO信息

## SEO管理模块详细需求Prompt

### 1. 功能需求

#### 1.1 SEO基础管理
- **文章SEO**：支持为文章设置SEO标题、关键词、描述
- **栏目SEO**：支持为栏目设置SEO标题、关键词、描述
- **专题SEO**：支持为专题设置SEO标题、关键词、描述
- **默认SEO**：支持设置站点默认SEO配置
- **Slug管理**：支持自动生成和手动设置URL友好的slug
- **Canonical标签**：支持设置规范链接，避免内容重复

#### 1.2 站点地图与爬虫规则
- **Sitemap生成**：自动生成XML格式的站点地图，包含文章、栏目、专题等URL
- **Robots.txt管理**：支持编辑和管理robots.txt文件，控制搜索引擎爬虫行为
- **重定向规则**：支持设置301重定向规则，处理URL变更

#### 1.3 SEO分析与优化
- **SEO预览**：支持预览页面在搜索引擎中的展示效果
- **SEO建议**：提供基本的SEO优化建议
- **关键词管理**：支持管理常用关键词

### 2. 技术实现

#### 2.1 数据模型
- **SeoMeta**：SEO元数据值对象，包含Title、Keywords、Description等字段
- **SeoRedirect**：SEO重定向实体，包含OldUrl、NewUrl、IsPermanent等字段
- **Website**：站点实体，包含默认SEO配置字段
- **Article/Channel/Topic**：各实体包含SEO相关字段

#### 2.2 服务层
- **ISEOService**：SEO服务接口，定义SEO相关的业务逻辑方法
- **SEOService**：SEO服务实现，包含SEO配置管理、站点地图生成、robots.txt管理等功能

#### 2.3 API接口
- **GET /api/seo/settings**：获取SEO设置
- **PUT /api/seo/settings**：更新SEO设置
- **GET /api/seo/sitemap**：获取站点地图
- **GET /api/seo/robots**：获取robots.txt内容
- **PUT /api/seo/robots**：更新robots.txt内容
- **POST /api/seo/redirects**：添加重定向规则
- **GET /api/seo/redirects**：获取重定向规则列表
- **DELETE /api/seo/redirects/{id}**：删除重定向规则

#### 2.4 前台实现
- **SEO标签输出**：在前台页面中输出完整的SEO标签
- **Sitemap.xml**：提供可访问的站点地图XML文件
- **Robots.txt**：提供可访问的robots.txt文件
- **Canonical标签**：在页面中输出规范链接标签

### 3. 数据隔离
- 所有SEO配置查询和操作都必须按WebsiteId进行隔离
- SEO缓存键必须包含WebsiteId，例如：website:1:seo:settings, website:1:seo:sitemap

### 4. 缓存策略
- SEO设置和站点地图数据需要缓存
- 文章、栏目、专题的SEO信息需要缓存
- SEO配置更新时，需要清理相关缓存

### 5. 前端实现
- **管理后台**：使用Vue实现SEO管理界面，包括SEO设置、重定向规则管理等功能
- **前台展示**：使用Razor视图实现SEO标签的输出

### 6. 测试策略
- **单元测试**：测试SEO服务的核心方法
- **集成测试**：测试SEO API接口的功能
- **功能测试**：测试SEO管理的完整流程，包括SEO设置、站点地图生成等
- **性能测试**：测试站点地图生成和SEO标签输出的性能

### 7. 技术要点
- **Slug生成**：自动将标题转换为URL友好的slug格式
- **站点地图生成**：定期自动生成站点地图，保证搜索引擎及时索引
- **robots.txt管理**：提供友好的编辑界面，方便管理爬虫规则
- **重定向规则**：支持正则表达式匹配，灵活处理URL变更
- **缓存优化**：合理使用缓存提高SEO相关查询性能
- **SEO最佳实践**：遵循搜索引擎优化最佳实践，提高站点排名
- **响应式设计**：SEO管理界面适配不同设备

### 10. 用户/角色/权限管理模块
- 用户管理
- 角色管理
- 菜单权限
- 按钮权限
- 栏目范围权限
- 建议角色：超级管理员、编辑、审核/发布人员、运营人员
- 技术要点：采用RBAC，叠加栏目范围控制，对新闻站来说，栏目级数据权限很实用

### 11. 操作日志/审计日志模块
- 登录日志
- 文章操作日志
- 发布日志
- 删除日志
- 权限变更日志
- 站点/栏目配置修改日志
- 技术要点：发布、删除、下线必须留痕，方便排查问题和责任追踪

### 12. 搜索模块
- 前台站内搜索
- 后台文章搜索
- 标题/摘要/标签检索
- 技术要点：一期可以先用数据库搜索，正文保存PlainText，后续数据量大了再升级全文检索

### 13. 前台页面渲染
- 首页展示（焦点图、栏目新闻区块、热门推荐）
- 栏目列表页（分页、筛选）
- 文章详情页（面包屑、正文、上一篇/下一篇、相关阅读）
- 标签聚合页
- 专题页
- 搜索结果页
- 404页面
- sitemap.xml
- robots.txt
- 响应式设计

### 14. 缓存策略
- MemoryCache（单机部署，不需要Redis）
- 适合缓存的内容：首页数据、栏目树、栏目列表、文章详情、推荐位、热门文章、标签页聚合
- 缓存失效建议：文章发布后，清理文章详情缓存、所属栏目页缓存、首页推荐缓存、标签页缓存、专题页缓存
- 缓存键规则：必须包含WebsiteId，例如：website:1:home, website:1:channel:12:list:1, website:2:article:345, website:3:tag:ai

### 15. 富文本清洗
- 目的：防XSS、清除Word垃圾HTML、去掉无用style/font/span、保证前台样式稳定
- 放置位置：Application层，在保存ArticleContent前执行
- 推荐流程：编辑器HTML → 清洗HTML → 提取PlainText → 保存ArticleContent
- 建议保存字段：HtmlContent、PlainText、WordCount

### 16. 草稿与发布分离
- 建议至少支持状态：草稿、待审核（可选）、已发布、已下线
- 技术要点：编辑中的内容不能直接影响线上，发布动作要明确，可逐步增加版本或发布快照

### 17. URL与路由设计
- 建议提前固定URL规则：
  - 首页：/
  - 栏目页：/news/
  - 文章页：/news/2026/03/12/12345.html
  - 专题页：/special/ai-2026/
  - 标签页：/tag/dotnet/
- 技术要点：URL稳定优先，slug规则统一，后期改URL成本很高

### 11. 操作日志/审计日志模块
- 登录日志
- 文章操作日志
- 发布/下线日志
- 删除日志
- 权限变更日志
- 站点/栏目配置修改日志
- 技术要点：发布、删除、下线必须留痕，方便排查问题和责任追踪

## 操作日志/审计日志模块详细需求Prompt

### 1. 功能需求

#### 1.1 登录日志管理
- **登录记录**：记录用户登录系统的操作，包括登录时间、IP地址、登录状态（成功/失败）、登录消息
- **登出记录**：记录用户登出系统的操作
- **登录统计**：支持按时间段统计登录次数、失败次数
- **登录日志查询**：支持按用户名、IP地址、时间范围、登录状态等条件查询登录日志
- **登录日志导出**：支持导出登录日志数据

#### 1.2 操作日志管理
- **操作记录**：记录用户在系统中的关键操作，包括操作类型、操作内容、操作时间、操作用户、IP地址
- **操作类型分类**：
  - 文章操作：创建、编辑、发布、下线、删除
  - 栏目操作：创建、编辑、删除、排序
  - 标签操作：创建、编辑、删除
  - 专题操作：创建、编辑、删除、文章关联
  - 推荐位操作：创建、编辑、删除、项目管理
  - 媒体资源操作：上传、删除
  - 用户管理操作：创建、编辑、删除、角色分配
  - 角色权限操作：创建、编辑、删除、权限分配
  - 站点配置操作：站点信息修改、SEO配置修改
- **操作日志查询**：支持按操作类型、操作用户、时间范围、关键词等条件查询操作日志
- **操作日志详情**：支持查看单条操作日志的详细信息
- **操作日志导出**：支持导出操作日志数据

#### 1.3 审计追踪
- **关键操作留痕**：对发布、删除、下线等关键操作必须记录详细日志
- **责任追踪**：通过操作日志可以追溯到具体的操作人员、操作时间和操作内容
- **异常操作告警**：支持配置异常操作检测规则，如短时间内大量删除操作
- **日志保留策略**：支持配置日志保留时间，自动清理过期日志

### 2. 技术实现

#### 2.1 数据模型
- **CmsOperationLog**：操作日志实体，包含Id、UserId、OperationType、OperationContent、IpAddress、UserAgent、CreatedAt、UpdatedAt、IsDeleted等字段
- **CmsLoginLog**：登录日志实体，包含Id、UserId、Username、Ip、UserAgent、Status、Message、CreatedAt等字段

#### 2.2 服务层
- **IOperationLogService**：操作日志服务接口，定义操作日志相关的业务逻辑方法
  - `GetListAsync`：获取操作日志列表，支持分页和筛选
  - `GetByIdAsync`：根据ID获取操作日志详情
  - `CreateLogAsync`：创建操作日志
  - `GetOperationTypesAsync`：获取所有操作类型列表
  - `ExportLogsAsync`：导出操作日志
  - `ClearOldLogsAsync`：清理过期日志
- **OperationLogService**：操作日志服务实现
- **ILoginLogService**：登录日志服务接口
  - `LogLoginAsync`：记录登录日志
  - `GetRecentLoginLogsAsync`：获取用户最近的登录日志
  - `GetLoginLogListAsync`：获取登录日志列表，支持分页和筛选
  - `GetLoginStatisticsAsync`：获取登录统计信息
  - `ExportLoginLogsAsync`：导出登录日志
- **LoginLogService**：登录日志服务实现

#### 2.3 API接口
- **操作日志API**：
  - `GET /api/operation-logs`：获取操作日志列表，支持分页和筛选（操作类型、用户、时间范围、关键词）
  - `GET /api/operation-logs/{id}`：根据ID获取操作日志详情
  - `GET /api/operation-logs/types`：获取所有操作类型列表
  - `POST /api/operation-logs/export`：导出操作日志
  - `DELETE /api/operation-logs/clear`：清理过期日志（管理员权限）
- **登录日志API**：
  - `GET /api/login-logs`：获取登录日志列表，支持分页和筛选（用户名、IP、时间范围、状态）
  - `GET /api/login-logs/statistics`：获取登录统计信息
  - `GET /api/login-logs/user/{userId}`：获取指定用户的登录日志
  - `POST /api/login-logs/export`：导出登录日志

### 3. 数据隔离
- 操作日志和登录日志属于全局数据，不进行WebsiteId隔离
- 但在查询时可以通过UserId关联到具体用户，通过用户关联到站点

### 4. 缓存策略
- 操作日志和登录日志数据一般不需要缓存，因为数据实时性要求高
- 可以缓存操作类型列表等静态数据

### 5. 前端实现
- **管理后台**：使用Vue实现操作日志管理界面，包括日志列表、详情查看、筛选查询、导出功能
- **日志展示**：使用表格展示日志列表，支持排序、分页、筛选
- **详情查看**：支持查看单条日志的详细信息，以弹窗或抽屉形式展示

### 6. 测试策略
- **单元测试**：测试操作日志服务的核心方法
- **集成测试**：测试操作日志API接口的功能
- **功能测试**：测试操作日志记录的完整流程，包括登录、文章操作、权限变更等
- **性能测试**：测试日志查询的性能，特别是大数据量下的分页查询

### 7. 技术要点
- **异步记录**：操作日志记录采用异步方式，避免影响主业务流程性能
- **批量插入**：支持批量记录操作日志，提高性能
- **日志级别**：支持不同级别的日志记录，如INFO、WARN、ERROR
- **敏感信息脱敏**：在记录操作内容时，对敏感信息进行脱敏处理
- **日志保留**：支持配置日志保留策略，自动清理过期日志
- **责任追踪**：通过完整的日志记录，实现操作责任的追踪和审计

## 用户/角色/权限管理模块详细需求Prompt

### 1. 功能需求

#### 1.1 用户管理
- **用户注册**：支持管理员创建用户，包括用户名、密码、邮箱、显示名称等信息
- **用户信息管理**：支持修改用户邮箱、显示名称、启用状态等基本信息
- **用户列表查询**：支持分页查询用户列表，支持按用户名、显示名称关键词搜索
- **用户详情查看**：支持查看用户详细信息，包括用户角色、权限等
- **用户删除**：支持软删除用户，删除后用户无法登录
- **用户状态管理**：支持启用/禁用用户，禁用后用户无法登录
- **密码修改**：支持管理员重置用户密码
- **用户角色分配**：支持为用户分配多个角色
- **用户权限查询**：支持查询用户拥有的所有权限

#### 1.2 角色管理
- **角色创建**：支持创建角色，包括角色名称、角色描述、启用状态等
- **角色信息管理**：支持修改角色名称、角色描述、启用状态等信息
- **角色列表查询**：支持分页查询角色列表，支持按角色名称、描述关键词搜索
- **角色详情查看**：支持查看角色详细信息，包括角色权限、关联用户等
- **角色删除**：支持软删除角色，删除后角色无法使用
- **角色状态管理**：支持启用/禁用角色
- **角色权限分配**：支持为角色分配多个权限
- **角色权限查看**：支持查看角色拥有的所有权限
- **角色用户查询**：支持查询拥有该角色的所有用户

#### 1.3 权限管理
- **权限创建**：支持创建权限，包括权限名称、权限代码、权限描述等
- **权限信息管理**：支持修改权限名称、权限代码、权限描述等信息
- **权限列表查询**：支持分页查询权限列表，支持按权限名称、代码、描述关键词搜索
- **权限详情查看**：支持查看权限详细信息，包括关联角色等
- **权限删除**：支持软删除权限，删除后权限无法使用
- **权限分类**：支持按模块分类权限（如文章管理、栏目管理、用户管理等）
- **权限代码规范**：权限代码采用模块.操作格式，如article.create、article.update等

#### 1.4 栏目权限管理
- **栏目权限分配**：支持为角色分配可管理的栏目权限
- **栏目权限继承**：支持父栏目权限继承到子栏目
- **栏目权限查询**：支持查询角色可管理的栏目列表
- **栏目权限验证**：在文章操作时验证用户是否有对应栏目的操作权限

### 2. 权限设计

#### 2.1 权限分类
系统权限按模块分类如下：

**文章管理权限**
- `article.view` - 查看文章
- `article.create` - 创建文章
- `article.update` - 编辑文章
- `article.delete` - 删除文章
- `article.publish` - 发布文章
- `article.offline` - 下线文章

**栏目管理权限**
- `channel.view` - 查看栏目
- `channel.create` - 创建栏目
- `channel.update` - 编辑栏目
- `channel.delete` - 删除栏目
- `channel.sort` - 栏目排序

**标签管理权限**
- `tag.view` - 查看标签
- `tag.create` - 创建标签
- `tag.update` - 编辑标签
- `tag.delete` - 删除标签

**专题管理权限**
- `topic.view` - 查看专题
- `topic.create` - 创建专题
- `topic.update` - 编辑专题
- `topic.delete` - 删除专题

**媒体资源管理权限**
- `media.view` - 查看媒体资源
- `media.upload` - 上传媒体资源
- `media.delete` - 删除媒体资源

**推荐位管理权限**
- `recommend.view` - 查看推荐位
- `recommend.create` - 创建推荐位
- `recommend.update` - 编辑推荐位
- `recommend.delete` - 删除推荐位

**用户管理权限**
- `user.view` - 查看用户
- `user.create` - 创建用户
- `user.update` - 编辑用户
- `user.delete` - 删除用户
- `user.assign_role` - 分配角色

**角色管理权限**
- `role.view` - 查看角色
- `role.create` - 创建角色
- `role.update` - 编辑角色
- `role.delete` - 删除角色
- `role.assign_permission` - 分配权限

**站点管理权限**
- `website.view` - 查看站点
- `website.create` - 创建站点
- `website.update` - 编辑站点
- `website.delete` - 删除站点

**系统管理权限**
- `system.config` - 系统配置
- `system.log` - 查看日志
- `system.backup` - 系统备份

#### 2.2 预设角色
系统预设以下角色：

**超级管理员**
- 拥有所有权限
- 可以管理所有站点、所有栏目
- 可以创建和管理其他角色

**站点管理员**
- 拥有站点管理权限
- 可以管理指定站点的所有栏目
- 可以管理站点的文章、标签、专题等

**编辑**
- 拥有文章、标签、专题的查看和编辑权限
- 可以创建和编辑文章、标签、专题
- 不能删除内容

**审核员**
- 拥有文章的查看和审核权限
- 可以发布和下线文章
- 不能创建和编辑文章

**访客**
- 只拥有查看权限
- 可以查看文章、栏目、标签、专题等

### 3. 技术实现

#### 3.1 数据库设计
- **CmsUser** - 用户表
- **CmsRole** - 角色表
- **CmsPermission** - 权限表
- **CmsUserRole** - 用户角色关联表（多对多）
- **CmsRolePermission** - 角色权限关联表（多对多）
- **CmsRoleChannel** - 角色栏目关联表（多对多）

#### 3.2 权限验证机制
- **基于角色的访问控制（RBAC）**：用户通过角色获得权限
- **权限缓存**：用户权限信息缓存到内存，提高验证性能
- **权限验证中间件**：在API请求时验证用户权限
- **栏目权限验证**：在文章操作时验证用户是否有对应栏目的操作权限

#### 3.3 密码安全
- **BCrypt加密**：使用BCrypt算法加密用户密码
- **密码强度验证**：验证密码强度（长度、复杂度等）
- **密码重置**：支持管理员重置用户密码

#### 3.4 缓存策略
- **权限缓存**：用户权限信息缓存1小时
- **角色缓存**：角色信息缓存30分钟
- **栏目权限缓存**：角色栏目权限缓存30分钟

### 4. API设计

#### 4.1 用户管理API
- `GET /api/users` - 获取用户列表
- `GET /api/users/{id}` - 获取用户详情
- `POST /api/users` - 创建用户
- `PUT /api/users/{id}` - 更新用户信息
- `DELETE /api/users/{id}` - 删除用户
- `GET /api/users/{id}/roles` - 获取用户角色
- `POST /api/users/{id}/roles` - 分配用户角色
- `POST /api/users/{id}/check-permission` - 检查用户权限
- `POST /api/users/{id}/reset-password` - 重置用户密码

#### 4.2 角色管理API
- `GET /api/roles` - 获取角色列表
- `GET /api/roles/{id}` - 获取角色详情
- `POST /api/roles` - 创建角色
- `PUT /api/roles/{id}` - 更新角色信息
- `DELETE /api/roles/{id}` - 删除角色
- `GET /api/roles/{id}/permissions` - 获取角色权限
- `POST /api/roles/{id}/permissions` - 分配角色权限
- `GET /api/roles/{id}/channels` - 获取角色可管理的栏目
- `POST /api/roles/{id}/channels` - 分配角色栏目权限

#### 4.3 权限管理API
- `GET /api/permissions` - 获取权限列表
- `GET /api/permissions/{id}` - 获取权限详情
- `POST /api/permissions` - 创建权限
- `PUT /api/permissions/{id}` - 更新权限信息
- `DELETE /api/permissions/{id}` - 删除权限
- `GET /api/permissions/categories` - 获取权限分类列表

### 5. 业务规则

#### 5.1 用户管理规则
- 用户名必须唯一，不能重复
- 邮箱必须唯一，不能重复
- 禁止删除超级管理员
- 禁止禁用超级管理员
- 用户删除后，关联的角色关系自动删除
- 用户禁用后，无法登录系统

#### 5.2 角色管理规则
- 角色名称必须唯一，不能重复
- 禁止删除超级管理员角色
- 角色删除后，关联的用户关系自动删除
- 角色删除后，关联的权限关系自动删除
- 角色禁用后，拥有该角色的用户无法使用对应权限

#### 5.3 权限管理规则
- 权限代码必须唯一，不能重复
- 权限删除后，关联的角色关系自动删除
- 禁止删除系统预设权限
- 权限代码必须符合模块.操作格式

#### 5.4 栏目权限规则
- 角色可以管理多个栏目
- 栏目权限支持继承，父栏目权限自动继承到子栏目
- 用户必须拥有栏目的操作权限才能对栏目下的文章进行操作

### 6. 测试策略
- **单元测试**：测试用户、角色、权限服务的核心方法
- **集成测试**：测试用户、角色、权限API接口的功能
- **权限验证测试**：测试权限验证机制，确保未授权用户无法访问受保护的资源
- **并发测试**：测试多用户并发操作时的权限验证性能
- **缓存测试**：测试权限缓存机制，确保缓存失效后能正确重新加载

### 7. 技术要点
- **RBAC权限模型**：采用基于角色的访问控制模型，实现灵活的权限管理
- **权限缓存**：用户权限信息缓存到内存，提高权限验证性能
- **权限验证中间件**：在API请求时验证用户权限，确保安全性
- **密码加密**：使用BCrypt算法加密用户密码，确保密码安全
- **软删除**：用户、角色、权限采用软删除机制，保留历史数据
- **栏目权限继承**：支持父栏目权限继承到子栏目，简化权限管理
- **权限审计**：记录权限变更操作，方便审计和追溯

## 数据库配置

### 连接字符串
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=CmsDb;Trusted_Connection=True;MultipleActiveResultSets=true"
  }
}
```

### 缓存配置
```csharp
builder.Services.AddMemoryCache();
builder.Services.AddSingleton<ICacheService, MemoryCacheService>();
```

## 路由设计

### 前台路由
- `/` - 首页
- `/{channelSlug}` - 栏目列表页
- `/{channelSlug}/{year:int}/{month:int}/{day:int}/{id}.html` - 文章详情页
- `/tag/{tagSlug}` - 标签聚合页
- `/special/{topicSlug}` - 专题页
- `/search` - 搜索页
- `/sitemap.xml` - 站点地图
- `/robots.txt` - 爬虫规则

### 后台API路由
- `/api/auth/*` - 认证相关
- `/api/articles/*` - 文章管理
- `/api/channels/*` - 栏目管理
- `/api/tags/*` - 标签管理
- `/api/topics/*` - 专题管理
- `/api/media/*` - 媒体资源管理
- `/api/recommend/*` - 推荐位管理
- `/api/users/*` - 用户管理
- `/api/roles/*` - 角色管理
- `/api/websites/*` - 站点管理

## 已实现的服务

### 应用服务层
- `IArticleService` / `ArticleService` - 文章服务
- `IUserService` / `UserService` - 用户服务
- `IChannelService` / `ChannelService` - 栏目服务
- `IMediaAssetService` / `MediaAssetService` - 媒体资源服务
- `IRecommendService` / `RecommendService` - 推荐位服务
- `ISEOService` / `SEOService` - SEO服务
- `IWebsiteService` / `WebsiteService` - 站点服务
- `ITagService` / `TagService` - 标签服务
- `ITopicService` / `TopicService` - 专题服务

### 基础设施服务层
- `ICacheService` / `MemoryCacheService` - 缓存服务
- `CmsDbContext` - 数据库上下文

## 部署方案

### 服务器环境
- Linux服务器
- 建议使用：Nginx、.NET 8 Runtime、systemd
- 数据库可单独部署，也可同机部署（看规模）

### 部署形态
建议部署为两个独立站点：

**管理后台站点**
- 一个独立站点，用于后台管理
- 例如：admin.yourdomain.com
- 职责：访问Vue管理后台、调用WebApi、后台管理、文章发布、资源上传、权限管理
- 这部分是独立后台入口，不对外开放普通前台访问

**Web前台站点**
- 一个独立站点，用于新闻网站前台展示
- 可以同时绑定多个域名：www.sitea.com、www.siteb.com、www.sitec.com
- 这些域名都指向同一个Web前台程序
- 也就是说：程序是一套，域名可以有多个，根据请求域名识别当前属于哪个Website，再按Website读取对应的数据和配置

### 前台多域名绑定方案
这是站群新闻站的关键点：

**核心思路**
- 同一个Cms.Web程序绑定多个域名
- 每次请求进入系统时：
  1. 读取请求Host
  2. 根据Host查询Website
  3. 获取当前WebsiteId
  4. 后续所有前台数据查询，都按WebsiteId过滤

**示例**
- www.sitea.com -> WebsiteId = 1
- www.siteb.com -> WebsiteId = 2
- www.sitec.com -> WebsiteId = 3

这样虽然程序只有一套，但能根据不同域名展示不同站点内容。

### 数据隔离要求
既然前台要根据域名区分站点内容，那么数据库层面就不能只在文章表里随便区分一下，而是要明确站点级数据隔离。

**建议这些表带WebsiteId**：
- Channels
- Articles
- Tags
- Topics
- RecommendSlots
- RecommendItems
- MediaAssets
- SeoRedirects
- WebsiteSettings

ArticleContents可以不带WebsiteId，因为它可以通过ArticleId反查所属站点。

### 查询规则
前台任何查询都必须带WebsiteId过滤：
- 栏目列表
- 文章详情
- 标签页
- 专题页
- 推荐位
- 热门文章
- sitemap
- robots
- SEO配置

否则会出现跨站串数据问题。

### 缓存规则
缓存key也必须带WebsiteId：
- website:1:home
- website:1:channel:12:list:1
- website:2:article:345
- website:3:tag:ai

否则不同域名命中同一份缓存，会直接串站。

### 站点识别实现建议
建议在前台项目里做一个CurrentWebsiteResolver或中间件：
- 职责：从HttpContext.Request.Host取域名、查询Website配置、得到当前WebsiteId、存入当前请求上下文、后续应用层/查询层统一读取
- 这样整个前台查询链路都能基于当前站点工作

### Nginx部署建议
**管理后台**
- 独立server配置，反向代理到后台应用

**Web前台**
- 一个前台程序可配置多个server_name，或者多个server指向同一个upstream
- 例如思路上是：多个域名都进同一个Cms.Web，Cms.Web内部根据Host判断Website
- 这样最符合"多域名 + 单前台程序"的目标

### 应用进程建议
建议后台和前台分开部署成两个systemd服务：
- cms-admin.service
- cms-web.service

**好处**：
- 前后台独立重启
- 日志独立
- 部署更清晰
- 资源隔离更好

### 这种部署方式的优点
这个方案非常适合当前目标：
- 后台独立，安全边界清晰
- 前台只有一套程序，维护成本低
- 多域名绑定方便
- 支持站群
- 不容易出现多套代码分叉
- 模板固定的前提下，数据驱动差异最合理

## 后续可改进方向

### 功能扩展
1. **评论系统**: 添加文章评论功能
2. **会员系统**: 实现用户注册、登录、个人中心
3. **多语言支持**: 实现国际化功能
4. **复杂审批流**: 添加文章审核流程
5. **统计分析**: 实现访问统计、用户行为分析
6. **广告管理系统**: 完善广告位管理

### 性能优化
1. **CDN集成**: 静态资源CDN加速
2. **图片压缩**: 自动图片压缩和优化
3. **数据库优化**: 添加索引、查询优化
4. **分布式缓存**: Redis集群支持（当需要扩展时）

### 安全增强
1. **HTTPS强制**: 启用HTTPS
2. **防CSRF**: 添加CSRF保护
3. **防XSS**: 输入输出过滤
4. **敏感词检测**: 内容审核功能

### 运维支持
1. **日志系统**: 完善日志记录
2. **监控告警**: 系统监控和告警
3. **备份恢复**: 数据库备份和恢复
4. **部署脚本**: 自动化部署脚本

## 开发说明

### 运行项目
1. 确保SQL Server LocalDB或MySQL已安装
2. 运行`dotnet build`编译项目
3. 运行`dotnet run --project Cms.Web`启动前台
4. 运行`dotnet run --project Cms.WebApi`启动后台API

### 数据库迁移
```bash
dotnet ef migrations add InitialCreate --project Cms.Infrastructure --startup-project Cms.Web
dotnet ef database update --project Cms.Infrastructure --startup-project Cms.Web
```

### 依赖安装
```bash
dotnet restore
```

## 总结

本项目成功实现了一个功能完整的新闻站引擎，涵盖了内容管理、用户认证、权限管理、SEO优化等核心功能。系统架构清晰，代码结构合理，易于维护和扩展。通过采用固定模板和简化运营动作的设计理念，系统具有良好的稳定性和可维护性。

特别支持多域名站群功能，通过单一程序实例绑定多个域名，根据域名自动识别站点并隔离数据，实现了高效的站群管理。

系统采用MemoryCache进行缓存，优化了前台性能，同时通过EF Core和Dapper的职责分离，保证了数据访问的效率和可维护性。

本系统非常适合新闻站和资讯站的使用场景，为内容管理和发布提供了稳定可靠的解决方案。